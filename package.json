{
  "name": "@proca/metaproxy",
  "version": "0.1.1",
  "description": "Metabase proxy that checks parameters vs Proca credentials",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@proca/api": "^0.2.3",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "jwks-rsa": "^2.0.2",
    "node-cache": "^5.1.2",
    "node-fetch": "^2.6.1",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "yarn build",
    "build": "npx tsc",
    "watch": "npx tsc -w"
  },
  "bin": {
    "metaproxy": "bin/metaproxy"
  },
  "files": [
    "bin",
    "dist",
    "README.MD"
  ],
  "devDependencies": {
    "@types/graphql": "^14.5.0",
    "@types/node-fetch": "^2.5.8",
    "graphql": "^15.5.0"
  }
}
